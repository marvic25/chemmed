# ChemMed — Editor Molecular UFPE

App web de química médica. Arquivo principal: `build5.py` → gera `chemmed5.html`.

Para gerar o HTML: `python3 build5.py`
Dependência: `npm install smiles-drawer` (em `/tmp/chemapp/` ou local)

Desenvolvido por Msc. Marcos Gregório — UFPE Química Medicinal.
